# ChatKit environment configuration
# Fill these in before running the app

# Required: Server-side key used by /api/create-session to call OpenAI
OPENAI_API_KEY=sk-replace_me

# Required: Published workflow ID from Agent Builder (looks like wf_...)
NEXT_PUBLIC_CHATKIT_WORKFLOW_ID=wf_replace_me

# Optional: Override the default API base (keep default unless instructed)
# CHATKIT_API_BASE=https://api.openai.com
